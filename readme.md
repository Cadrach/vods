# VODs

## Installing

Install all libraries:

> ```composer install```

> ```npm install```

> ```bower install```

Then compile the project using **gulp**:

> ```gulp```

Then edit **.env**

## Developing

To keep .css up to date from sass:

> ```gulp watch```